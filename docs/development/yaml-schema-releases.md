# Edge16 YAML schema releases

This file is the source of truth for the published YAML schema release version.
Add a new top-level `## <version>` entry whenever generated schemas change.

## 1.0

Initial published schema set for Edge16 model and radio YAML files.

### Added

- Draft 2020-12 JSON Schemas for TX16S MK2 (`tx16s`) and TX16S MK3 (`tx16smk3`).
- Model and radio YAML roots.
- Schema locator comment convention for radio-written YAML files.
